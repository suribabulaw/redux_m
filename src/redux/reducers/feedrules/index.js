// import external modules
import { combineReducers } from "redux";
// import internal(own) modules
import feedrules from "./feedrules";
//import feedRulesOption from './feedRulesOption'

export default combineReducers({
    feedrules,
    // options
});